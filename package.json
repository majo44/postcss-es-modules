{
    "name": "postcss-es-modules-monorepo",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "packages/*",
        "docs/examples/*"
    ],
    "scripts": {
        "build": "yarn workspaces run build"
    }
}
