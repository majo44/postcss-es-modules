{
    "name": "postcss-es-modules-monorepo",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "packages/*",
        "docs/examples/*"
    ],
    "scripts": {
        "build": "yarn workspaces run build",
        "compile": "yarn workspaces run compile",
        "test": "yarn workspaces run test",
        "lint": "yarn workspaces run lint"
    }
}
